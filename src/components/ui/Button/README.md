Button Component

args - variant(primary/secondary/destructive),
        sizes(sm, lg, md),
        disabled(true or false),
        label,
        loading,
        icon,
        iconPosition(left/right)


We are not going to handle the onClick handler here