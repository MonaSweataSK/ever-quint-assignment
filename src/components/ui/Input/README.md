Input

we are going for Inpit insetead of TextInput so that the component is generic and the same can be used for other inputs like number, date, checkbox etc 

args -text, placeholder, disabled, type
