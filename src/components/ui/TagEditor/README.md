this componnet is for adding tags inside a input field

something like this
the input field should contain - all the tags ( with a cross icon to remove them ).
in the input field, the user should be able to add many tags with a click of enter

Args -
  array of tags,
  placeholder,
  limit 